/* // OPERADORES

const variable = 'Valor de la variable'  // = es valor de asignacion

const suma = 1 + 3 //  + Operador aritmetico de la suna
const resta = 1 - 3 // -  Operador aritmetico de la resta
const multiplicacion = 1 * 3 // *  Operador aritmetico de la multiplicacion 
const division = 1 / 3 // / Operador aritmetico de la division
const resto = 5 % 2 // % operador aritmetico q nos dara el resto de una division
const exponente = 3 ** 3 // ** es el operador de la exponenciacion

let x = 5;
let y = 5;

x++; //aumentador
y--; //disminuidor



console.log(suma)
console.log(resta)
console.log(multiplicacion)
console.log(division)
console.log(resto)
console.log(exponente)

console.log(x);
console.log(y);

// OPERADORES LOGICOS


// BOOLEAN

let x = 5;
let y = '5';

let z = x == y; // el == compara solo los valores absolutos
let z = x === y; // el === compara valores y tipo de variable 
let z = x != y; //operador negacion (si x es distinto a y va a ser true, si x es igual a y retornara false)
let z = x > y; // operador mayor que
let z = x < y; // operador menor que
let z = x >= y; // operador mayor o igual que
let z = x <= y; // operador menor o igual que

let a = 5;
let b = 9;
let c = 5;

let x = a < b && b > c;  // && operador ampersand (y) : Si hay un false entonces sera false (si o si ambos resultados deben ser true para retornar true)
let x = a < b || b > c; // || operador  or (o) : Si uno de los resultados es verdaderos, entonces es verdadero. (si ambos son falsos retorna falso)
 

let x = a == 5 && b == 19; 

console.log(x);
 */